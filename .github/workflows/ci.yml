name: Verify AstraFlow

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:
    inputs:
      live_provider_validation:
        description: "Run GitHub Actions-token and optional Gemini provider health checks"
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  quality-gate:
    name: Typecheck, test, and build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.15.1
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm check

      - name: Run unit tests
        run: pnpm vitest run

      - name: Build production bundle
        run: pnpm build

  live-provider-contracts:
    name: Live provider contracts
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.live_provider_validation }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      # The live test reads this CI-specific variable before mapping it to GITHUB_TOKEN.
      ASTRAFLOW_LIVE_GITHUB_TOKEN: ${{ github.token }}
      RUN_LIVE_PROVIDER_TESTS: "true"
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.15.1
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate configured providers
        run: pnpm vitest run server/integrationHealth.live.test.ts
